plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'com.fullcyle.admin.catalog.application'


dependencies {
    implementation(project(":domain"))
    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
